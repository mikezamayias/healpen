import 'package:flutter/material.dart';

class JournalLengthTile extends StatelessWidget {
  const JournalLengthTile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Journal Length');
  }
}
