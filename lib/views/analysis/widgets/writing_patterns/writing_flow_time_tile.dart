import 'package:flutter/material.dart';

class WritingFlowTimeTile extends StatelessWidget {
  const WritingFlowTimeTile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Writing Flow Time');
  }
}
