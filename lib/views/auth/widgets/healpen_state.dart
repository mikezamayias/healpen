import 'package:flutter/material.dart';

import '../../../healpen.dart';

class HealpenState extends StatelessWidget {
  const HealpenState({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Healpen();
  }
}
